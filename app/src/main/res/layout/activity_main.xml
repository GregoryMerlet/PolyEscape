<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/primaryLightColor">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="20dp">
        <ImageView
            android:id="@+id/logoMain"
            android:layout_width="wrap_content"
            android:layout_height="150dp"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="30dp"
            android:src="@drawable/logo_rect"
            android:contentDescription="@string/logo" />

        <TextView
            android:id="@+id/textMain"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/logoMain"
            android:text="Bienvenue sur l'application PolyEscape. Pour commencer scannez le QR code du maitre du jeu."
            android:textColor="@color/primaryTextColor"
            android:layout_marginBottom="10dp"
            android:textSize="40sp"
            android:textAlignment="center"/>

        <ImageButton
            android:id="@+id/qrScanMainButton"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/textMain"
            android:src="@drawable/ic_qr_code_scan"
            android:background="@drawable/orange_button"/>
    </RelativeLayout>

</RelativeLayout>